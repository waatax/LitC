import fs from 'fs'; import OpenCC from 'opencc-js'; const p='./src/data/readingAid.ts'; const conv=OpenCC.ConverterFactory(...OpenCC.Locale.from.cn); const lines=fs.readFileSync(p,'utf8').split(/\r?\n/); let n=0; for(let i=0;i<lines.length;i++){const x=conv(lines[i]);if(x!==lines[i]){lines[i]=x;n++;}} fs.writeFileSync(p,lines.join('\n'),'utf8'); console.log(JSON.stringify({changedLines:n}));
