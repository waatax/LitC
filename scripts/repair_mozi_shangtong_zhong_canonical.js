import fs from'node:fs';const file='src/data/works.ts';let source=fs.readFileSync(file,'utf8'),m=[...source.matchAll(/JSON\.parse\(decodeURIComponent\("([^"]+)"\)\)/g)],d=m.map(x=>JSON.parse(decodeURIComponent(x[1]))),[w,c,p,s]=d;const fixes={'mo-zi_ch-12_p-1':t=>t.replace('故相交非也','故交相非也'),'mo-zi_ch-12_p-7':t=>t.replace('是以謀事，舉事成','是以謀事得，舉事成'),'mo-zi_ch-12_p-8':()=> '今天下之人曰：「方今之時，天下之正長猶未廢乎天下也，而天下之所以亂者，何故之以也？」'};for(const[id,fn]of Object.entries(fixes)){const x=p.find(y=>y.id===id);x.canonicalText=fn(x.canonicalText);for(let i=s.length-1;i>=0;i--)if(s[i].passageId===id)s.splice(i,1);const a=x.canonicalText.match(/[^。！？]+[。！？]/g)??[x.canonicalText],r=a.map((canonicalText,i)=>{const order=i+1,sid=`${id}_s-${order}`,cue=[...canonicalText][0]??'';return{id:sid,passageId:id,order,canonicalText,cue,chunks:[{id:`${sid}_c-1`,sentenceId:sid,order:1,text:canonicalText,cue}]}});x.sentenceIds=r.map(y=>y.id);s.push(...r)}s.sort((a,b)=>a.passageId.localeCompare(b.passageId,'en',{numeric:true})||a.order-b.order);const mo=w.find(x=>x.id==='mo-zi'),ids=new Set(c.filter(x=>x.workId==='mo-zi').map(x=>x.id));mo.totalChars=p.filter(x=>ids.has(x.chapterId)).reduce((n,x)=>n+x.canonicalText.length,0);for(let i=m.length-1;i>=0;i--){const x=m[i],e=encodeURIComponent(JSON.stringify(d[i]));source=`${source.slice(0,x.index)}JSON.parse(decodeURIComponent("${e}"))${source.slice(x.index+x[0].length)}`}fs.writeFileSync(file,source,'utf8');console.log('Repaired 墨子·尚同中 passages 1, 7, and 8.')
